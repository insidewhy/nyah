IdentifierTail = [A-Za-z0-9_]*

ClassName = [A-Z] ^ IdentifierTail
RuleName  = [a-z] ^ IdentifierTail

Attribute {
    begin = TODO
}

RuleValue {
    prefix = enum 'key' | 'local'
    begin = TODO
}

Attribute = TODO

Class : ClassPrefix {
    key className   = ClassName
    contents        = TODO

    begin = prefix:'skip'? className ~ 
            '=' > RuleValue |
            '{' >> contents '}'

}

Global = Class*
