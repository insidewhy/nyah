IdentifierTail = [A-Za-z0-9_]*

ClassName = [A-Z] ^ IdentifierTail
RuleName  = [a-z] ^ IdentifierTail

Attribute {
    begin = TODO
}

RuleValue {
    prefix = 'key' | 'skip'
    begin = TODO
}

Attribute = TODO

Class : ClassPrefix {
    prefix          = 'skip' | 'store'
    key className   = ClassName
    contents        = TODO

    begin = prefix className ~ 
            '=' > RuleValue |
            '{' >> contents '}'

}

Global = Class*
