sequence: teg.range.Range => "friend"
sequence2: std.typecons.Tuple {
    teg.range.Range,
    teg.range.Range
} => (
    "friend",
    "baby"
)
many list: teg.range.Range[] => [
    "v1",
    "v2"
]
lexeme: teg.range.Range[] => [
    "v1",
    "v2"
]
store range: teg.range.Range => "var v1"
